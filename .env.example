PORT=3000
# OpenAI-compatible VPS endpoint, including /v1 if required by your server.
VPS_AI_BASE_URL=
VPS_AI_MODEL=
# Optional: use a protected local file containing only the API key.
VPS_AI_KEY_FILE=
# Alternatively set VPS_AI_API_KEY in your environment.
